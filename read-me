To setup :

Run 'npm install'
Run 'npm start'

If time permits,
	Will do more styles to the application to feel better.
	Add the images depending on the screen resolution (mobile, tablet, desktop).
	Add some loading gifs.
	Clean and optimize the code and add few comments.
